{
	"name": "@trc/e2e",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"test": "vitest run --passWithNoTests",
		"test:docker": "vitest run --passWithNoTests src/docker.e2e.test.ts"
	},
	"dependencies": {
		"jose": "^5.6.0"
	}
}
