server:
  host: 0.0.0.0
  port: 3000
logging:
  level: debug
auth:
  type: jwt
  jwt:
    secret: a-string-secret-at-least-256-bits-long
storage:
  provider: local
  local:
    rootDir: /tmp/cache/turbo
