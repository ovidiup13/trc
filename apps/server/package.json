{
	"name": "@trc/server",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"start": "bun run src/index.ts",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"test": "vitest run --passWithNoTests"
	},
	"dependencies": {
		"@trc/config": "workspace:*",
		"@trc/provider-local": "workspace:*",
		"@trc/shared": "workspace:*",
		"@trc/storage-core": "workspace:*",
		"hono": "^4.6.0",
		"jose": "^5.6.0"
	}
}
